\section{Metodología}
\label{sec:metodologia}
\subsection{Morfología de las shells de la BCG de \clust{}} \label{sec:contorno}
El primer objetivo que nos debemos marcar es la delimitación de las regiones del espacio cercano a la BCG en las que existen shells. Como explicamos en la introducción, las shells se pueden considerar como excesos de luminosidad que alteran la curva de luz de una galaxia. Por lo tanto, una forma sencilla de aislar la luz producida por las shells es proponer un modelo \enquote{de primer orden} para el perfil luminoso de la BCG y considerar únicamente la luz sobrante, en lo que llamamos la imagen residual. El software GALFIT \citep{pengDetailedStructuralDecomposition2002, pengDETAILEDDECOMPOSITIONGALAXY2010} es capaz de ajustar los parámetros de dicho modelo y devolver una imagen, a priori, sin la luz de la BCG. El perfil de luz más común para una galaxia elíptica es el llamado perfil de Sérsic \citep{sersicInfluenceAtmosphericInstrumental1963,savorgnanSupermassiveBlackHole2013}, que tiene la siguiente forma \citep{pengDetailedStructuralDecomposition2002}:

\begin{equation}
    \Sigma (r) = \Sigma_e e^{-\kappa [(r/r_e)^{1/n} - 1]}
\end{equation}

donde \(\Sigma (r)\) representa el brillo superficial en el radio elíptico \(r\); \(r_e\) el radio que encierra la mitad de la luz emitida por la galaxia; \(\Sigma_e\) el brillo superficial en \(r_e\); \(n\) el índice de Sérsic, que determina la concentración de la luz (un índice más alto implica una transición más abrupta entre el núcleo brillante de la galaxia y el halo tenue, característico de las galaxias elípticas); y \(\kappa\) un parámetro dependiente de \(n\) que mantiene constante la relación de \(r_e\) con el flujo. \par

Siguiendo esta idea, ajustamos la BCG de \clust{} con un perfil de Sérsic elíptico y unos parámetros iniciales para su tamaño, forma y brillo obtenidos con la ayuda de SAOImage DS9. Para ello seleccionamos, por su resolución, las imágenes de JWST, y de entre ellas la del filtro F277W, por ser la más brillante que no presenta wisps. El modelo producido por el ajuste de GALFIT es una elipse con las siguientes características: centro situado a \(21^{\text{h}}29^{\text{m}}39^{\text{s}}.9573\) de ascensión recta y \(+00^{\circ} 05^{\prime}21^{\prime\prime}.24\) de declinación; \(r_e = 12,41 \: \text{arcsec} \: (46,20 \: \text{kpc})\); \(n = 3,03\); \(b/a = 0,51\) y \(\alpha = 64,75^{\circ}\); donde \(b/a\) es el cociente entre ejes de la elipse y \(\alpha\) el ángulo del eje mayor con respecto a la vertical. El ajuste tiene un \(\chi^{2}_{\nu} = 0,602\) y se mantiene estable ante grandes variaciones de los parámetros iniciales, lo que sugiere que es robusto. En la fila superior de la figura \ref{jwst277} incluimos, con la misma escala que en la figura \ref{bcg} (0-1 MJy/sr), la imagen de JWST de la BCG y el modelo de GALFIT. En la primera imagen observamos, además de las estructuras que ya comentamos para la figura \ref{bcg}, varios posibles casos de galaxias afectadas por \textit{lensing} (lentes gravitatorias). Al este de la BCG tenemos el ejemplo más claro, con una galaxia claramente duplicada y cuya luz forma un arco de anillo de curvatura amplia. Más sutiles son las dos galaxias que se encuentran al sur de la BCG, que, pese a que la imagen no es concluyente, parecen sufrir el mismo fenómeno. Por su parte, en el modelo de GALFIT tenemos una elipse cuya forma y brillo coinciden a grandes rasgos con los de la BCG, señal de que el ajuste es acertado. \par 

La tercera imagen de la figura se corresponde con el resultado de restar la elipse del modelo a la imagen original. Para resaltar las regiones de bajo brillo superficial, se representa con una escala de 0 a 0,2 MJy/sr. De inmediato se distingue una shell al este de la BCG, en la región donde están presentes las galaxias que ya veíamos antes. También se aprecia, en el lado opuesto, una zona luminosa mucho más delgada y tenue que cumple las características de una shell. Por lo tanto, este modelo simple permite identificar dos shells en la imagen del filtro F277W de JWST. Es probable que la luz restante al noroeste de la BCG también se corresponda con varias shells. De hecho, se intuye la presencia de picos y valles de luminosidad, característicos de estas estructuras. Sin embargo, en la imagen residual no es posible distinguir todo el arco de las shells, si es que este existe, por lo que no las vamos a considerar. Por último, en esta imagen también se destaca una de las galaxias (la más occidental) al sur de la BCG afectadas por \textit{lensing} que señalábamos en el párrafo anterior. La más oriental no tiene un anillo identificable en esta imagen, pero sí se distinguen los puntos más brillantes en los extremos (correspondientes con una imagen duplicada de la galaxia), lo que refuerza esta observación. \par


\begin{figure}[h]
\centering
\includegraphics[trim={0 0 0 0}, clip, width=0.45\textwidth]{jwst277.png}
\includegraphics[trim={0 0 0 0}, clip, width=0.45\textwidth]{jwst277_elipse.png} \\
\includegraphics[trim={0 0 0 0}, clip, width=0.5\textwidth]{jwst277_sinbcg.png}
\caption{Fila superior, izquierda: imagen ampliada de la BCG de \clust{} en el filtro F277W de JWST con una escala de 0-1 MJy/sr, descrita en el texto. Fila superior, derecha: modelo de GALFIT de la BCG de \clust{}, con la misma escala. Se observa un contorno y un brillo muy similar. Inferior: imagen residual resultante de restar la segunda imagen a la primera, con una escala de 0-0,2 MJy/sr. Como se indica en el texto, se aprecian claramente dos shells, y hay indicios de que existen más.  \label{jwst277}}
\end{figure}
\noindent

%Por lo tanto, llegados a este punto parece razonable plantear un análisis en el que se elimine la luz de la BCG en cada filtro y se midan las características de las shells aisladas. Sin embargo, en las imágenes de HST el ajuste es problemático. En la figura \ref{hst160} aplicamos el mismo procedimiento que en la figura \ref{jwst277} a la imagen del filtro F160W. En este caso, GALFIT modela una galaxia más alargada (\(b/a = 0,38\) ) que hace desaparecer la shell occidental y afecta gravemente al centro de la oriental. Además, no es capaz de eliminar satisfactoriamente la luz de la BCG al norte y al sur, por lo que tampoco es posible distinguir esta shell de la BCG. Este ajuste es estable bajo grandes variaciones de los parámetros iniciales y la aplicación de máscaras (zonas no tenidas en cuenta en el ajuste) en determinadas regiones. \par


%Esta dificultad obliga a cambiar de estrategia. En lugar de aislar las shells en cada filtro y medir las características de la imagen residual, 

Aplicando el mismo procedimiento para las demás imágenes de JWST, comprobamos que se obtienen resultados muy similares para todas ellas. Sin embargo, no se puede decir lo mismo para las imágenes de HST. Como detallamos en el anexo \ref{sec:anexo}, con dicho telescopio no es posible aislar adecuadamente la luz producida por las shells. \nota{añadiré la importancia de esto a las conclusiones}. Por lo tanto, en el análisis utilizaremos la imagen más nítida de JWST (por lo que ya vimos, la asociada al filtro F277W) para identificar las regiones que se corresponden con shells y mediremos el flujo sobre dichas regiones en todas las imágenes de fotometría y espectroscopía. La ventaja de este procedimiento, además de la facilidad que presenta para extenderlo a todas las imágenes de HST/ACS y MUSE, es la consistencia que otorga el trabajar con imágenes que no han sufrido un procesado adicional. El principal inconveniente es que las peculiaridades del flujo procedente de las shells pueden ser demasiado sutiles en comparación con el flujo total de la BCG, y por lo tanto podemos estar ocultando características fundamentales. \par

Siguiendo lo expuesto en el párrafo anterior, partimos de la imagen residual de F277W para modelar las formas de los contornos de las shells utilizando la función \textit{measure.find\_contours} de la librería \textit{scikit-image} de Python. En la figura \ref{contours} vemos, marcados en rojo, todos los contornos identificados por el código. De estos, hemos destacado en verde los que verdaderamente se corresponden con shells identificables.\par 

La shell oriental está situada a una distancia galactocéntrica mediana \nota{mejor la media?} de 6,6 arcsec (24,6 kpc) y tiene un área de 34,7 arcsec\(^{2}\) (480,7 kpc\(^{2}\)). Vemos que la parte exterior del contorno está bien definida y se ajusta a lo que esperamos de una shell, con una curvatura amplia pero constante. La única excepción se da en la zona más al sur, en la que una de las galaxias forma un \enquote{bulbo} que extiende la frontera del contorno. En la zona interior, sin embargo, la forma es más compleja. Al norte, las galaxias provocan el mismo fenómeno que acabamos de describir para la frontera exterior, pero de manera aún más acusada. En el centro, el modelo de la galaxia invade parte de la shell y genera una forma de asa, y al sur probablemente no elimina suficientemente la luz de la BCG e identifica como parte de la shell zonas más interiores. Este es esencialmente el mismo problema que teníamos al aplicar el modelo del GALFIT a las imágenes de HST, pero con consecuencias menos graves. Ya que no nos es posible determinar qué partes de la shell son \enquote{verdaderas} y cuáles son un artefacto del modelo, tomaremos este último como referencia y únicamente nos preocuparemos de eliminar la contribución de las galaxias. \par

La shell occidental, por su parte, está más alejada del centro, a una distancia de 9,6 arcsec (35,7 kpc). Como ya esperábamos, es mucho más pequeña, al tener un área de apenas 5,6 arcsec\(^{2}\) (77,6 kpc\(^{2}\)). Su forma es mucho más simple, al no tener ninguna galaxia en su interior ni estar tan cerca del centro galáctico. \par



\begin{figure}[h]
\centering
\includegraphics[trim={5 5 5 5}, clip, width=0.6\textwidth]{jwst277_contours.png}
\caption{Definición de los contornos obtenidos por \textit{scikit-image} sobre la tercera imagen de la figura \ref{jwst277}. Destacamos en verde los que se corresponden con shells identificables. Los contornos rojos rodean fundamentalmente otras galaxias. De estas, ayudan especialmente a ver con mayor claridad el \textit{lensing} de la galaxia que se encuentra al sur de la BCG. También se destacan las posibles shells no completas del oeste de la BCG. \label{contours}}
\end{figure}
\noindent

\subsection{Color de las shells} \nota{mover a resultados?} \label{sec:colorshells}
Una vez caracterizado el contorno de la shell, queremos medir sus propiedades físicas y compararlas con las de otras regiones a la misma distancia galactocéntrica. La primera aproximación que haremos será muy visual. Sabemos que existe una correlación significativa entre el índice de color de una población estelar y su composición química: cuanto mayor es su metalicidad, más roja es una galaxia \citep{bruzualStellarPopulationSynthesis2003,mihosStelLARPOPULATIONSOUTER2013,montesINTRACLUSTERLIGHTFRONTIER2014}. Por ello nos interesa elaborar un mapa de color de la BCG similar al de \citet[fig. 1]{mihosStelLARPOPULATIONSOUTER2013}, comparando el flujo medido por dos de los filtros que tenemos disponibles. No obstante, esta correlación está fuertemente degenerada con la edad de la población estudiada, de modo que una galaxia más antigua también es más roja. En este apartado, por lo tanto, no podemos separar ambos efectos, pero sí es de interés observar, en cualquier caso, si la shell tiene características particulares. \par

%El índice de color de un píxel se calcula a partir de la resta de magnitudes relativas asociadas a dos longitudes de onda características distintas

Escogemos filtros de HST/ACS para asegurarnos de que no introducimos distorsiones al comparar imágenes de cámaras distintas. De ellos, nos decantamos por F435W y F625W, por generar un contraste suficiente entre sus imágenes y tener anchos de banda similares. El índice de color F435W - F625W de cada píxel se obtiene restando las magnitudes AB (\(m_{AB}\)) de dicho píxel en las imágenes asociadas a cada uno de esos filtros. \(m_{AB}\) se calcula de la siguiente manera: en primer lugar, se obtiene la magnitud del punto cero ZP. Esta es la magnitud AB máxima que puede medir el telescopio, es decir, aquella asociada al flujo mínimo que este es capaz de detectar. En el caso de HST, la magnitud del punto cero es\footnote{\url{https://hst-docs.stsci.edu/acsdhb/chapter-5-acs-data-analysis/5-1-photometry}} 

\begin{equation}
    \text{ZP}_{\text{HST}} = -5\log_{10}{\text{(PHOTPLAM)}} - 2,5\log_{10}{\text{(PHOTFLAM)}} - 2,408 
\end{equation}

donde PHOTPLAM representa la longitud de onda de pivote \(\lambda\) del filtro (en \AA) y PHOTFLAM el flujo promedio (en \(\text{erg}\: \text{cm}^{-2}\: \text{s}^{-1}\: \text{\AA}^{-1}\)) necesario para registrar una cuenta por segundo en el detector, según se indica en ambos casos en el \textit{header} del archivo \textit{fits}. La magnitud final es

\begin{equation}
    m_{AB} = - 2,5\log_{10}{(\Phi)} + \text{ZP}
\end{equation}

donde \(\Phi\) representa el flujo sobre el píxel en \(\text{e}^{-}/\text{s}\). Evidentemente, al tratarse de cantidades logarítmicas, todos los píxeles con \(\Phi \leq 0\) en cada filtro (presentes especialmente en las zonas de bajo brillo superficial, debido al efecto de la sustracción del fondo en el tratamiento de las imágenes) no tendrán asociada una magnitud AB válida y, por lo tanto, no tendrán un índice de color asociado. \par

En la figura \ref{popmetal}, \citeauthor{popGalaxiesShellsIllustris2017} comparaba la metalicidad mediana de varios puntos situados sobre shells y de otros ajenos a ellas pero situados a las mismas distancias galactocéntricas. En nuestro caso trabajamos con medidas reales en las que tenemos incertidumbres y, como ya explicamos en el párrafo anterior, están presentes muchos píxeles con flujos negativos, por lo que en un primer lugar nos limitamos a un análisis visual. En el mapa presentamos los contornos de las shells, así como varias regiones de referencia con formas idénticas y situadas a las mismas distancias galactocéntricas, rotadas un cierto ángulo con respecto al centro de la BCG calculado por GALFIT. El ángulo es de \(\frac{3\pi}{5}\) en el caso de la shell oriental y de \(\frac{\pi}{3}\) en el caso de la occidental. También incluimos, siguiendo el mismo planteamiento, regiones \enquote{espejo} situadas en el extremo opuesto de la BCG para cada shell. En la figura \ref{color} vemos el mapa de color con los contornos de las shells representados en blanco, las regiones de referencia en naranja, y las regiones espejo en verde. Sobre ella se ha aplicado, con el objetivo de reducir el ruido visual de la imagen, un filtro gaussiano con la función \textit{ndimage.gaussian\_filter} de la librería de Python \textit{scipy} con \(\textit{sigma} = 0,6\). No obstante, este tiene el efecto de igualar los píxeles con índice 0 (donde \(\Phi_{\text{F435W}} = \Phi_{\text{F625W}}\)) con aquellos sin índice de color válido (\(\Phi_{\text{F435W}} \leq 0\) o \(\Phi_{\text{F625W}} \leq 0\)), por lo que en el anexo \ref{sec:colorsingauss} se adjunta el mismo mapa sin dicho filtro gaussiano. \par % ya que hacemos un ejercicio similar, pero, dado que estamos trabajando con una imagen real y necesitamos áreas mayores, utilizamos toda la superficie de las shells (con las galaxias enmascaradas) calculada en los apartados anteriores y medimos el índice de color F435W - F625W sobre ellas. las áreas sin shells utilizamos contornos de idéntica forma y situados a la misma distancia al centro galáctico, con una rotación con respecto a este de \(\frac{3\pi}{5}\) en el caso de la shell oriental y \(\frac{\pi}{3}\) en el de la occidental.


\begin{figure}[h]
\centering
\includegraphics[trim={5 0 5 5}, clip, width=0.7\textwidth]{color.png}
\caption{\nota{escribir pie de figura} \label{color}}
\end{figure}
\noindent

Es evidente que las regiones más rojas son las cercanas a los centros galácticos, lo que ya era esperable y se apreciaba de forma nítida en \citet[fig. 1]{mihosStelLARPOPULATIONSOUTER2013}. En cuanto a las shells, observamos un claro exceso de luminosidad del filtro rojo en la zona perteneciente a la shell oriental, especialmente en comparación con su región de referencia. Su región espejo también es visiblemente más azul, por lo que, si existen shells en esta zona que no pudimos detectar, estas no exhiben las mismas propiedades. Más al oeste, en la shell pequeña, el índice de color no es especialmente alto, pero sí destaca frente a su región de referencia al norte. También parece ser ligeramente más roja que su región espejo, pero esta diferencia es menos concluyente. En cualquier caso, la inspección visual parece indicar que, como indicaban las predicciones teóricas, las shells presentan un exceso de luminosidad en los filtros más rojos, al menos en el rango óptico. \par

\citeauthor{mihosStelLARPOPULATIONSOUTER2013} también realizaba un análisis consistente en medir el índice de color mediano a varias distancias galactocéntricas, en el cual la existencia de una zona especialmente roja es indicativa de la presencia shell. Nosotros vamos a hacer un análisis similar, en el que tomaremos dos sectores circulares de \(30^{\circ}\) de ancho y divididos en 14 segmentos radiales de 1,78 kpc, tal y como se muestra en la figura \ref{mapamihos}. En cada uno de los segmentos mediremos el índice de color F435W - F625W mediano y compararemos los resultados obtenidos para el sector orientado a la shell oriental (situado entre \(60^{\circ}\) y \(90^{\circ}\) sobre la vertical) y el orientado a su región de referencia (desplazado \(\frac{3\pi}{5}\)). Los resultados se muestran en la sección \ref{sec:mihos}.

\begin{figure}[h]
\centering
\includegraphics[trim={3 0 3 3}, clip, width=0.5\textwidth]{mihosmapa.png}
\caption{\nota{escribir pie de figura} \label{mapamihos}}
\end{figure}
\noindent

%Las medidas confirman estas apreciaciones. El índice de color mediano en el contorno de la shell, con las galaxias enmascaradas como se indica en el apartado \ref{sec:mascara}, es de 1,65, mientras que en la región de referencia (también enmascarada en lugares equivalentes) es de 1,32. 

\subsection{Método para el análisis espectrofotométrico de las shells} \label{sec:metodoespectro}
Siguiendo el camino marcado por el apartado anterior, el objetivo último de este trabajo es medir el brillo de las shells en todos los filtros que tenemos disponibles de HST y JWST y en todo el rango espectroscópico de MUSE. Las medidas en conjunto ofrecen un rango suficientemente amplio como para poder elaborar un modelo de su distribución espectral de energía (SED). En esta sección describiremos los pasos a seguir para llevarlas a cabo. \par

La medición del flujo en una región se hace según las pautas establecidas en el apartado anterior para calcular la magnitud de cada píxel en HST. La aplicación de dicho procedimiento a áreas más extensas es trivial para este telescopio. Una vez calculada la magnitud AB de un región, el resultado se ofrecerá en maggies, donde el flujo en maggies\footnote{\url{https://prospect.readthedocs.io/en/latest/dataformat.html}} \(\Phi_{mag} = 10^{-0,4 m_{AB}}\) y \(1 \: \text{maggie} = 1/3631 \: \text{Jy}\) \nota{cambio}. Al extender la medición a los otros dos telescopios, tenemos en cuenta que, para JWST

\begin{equation}
     \text{ZP}_{\text{JWST}} = -6,10 - 2,5\log_{10}{\text{(PIXELSCALE}^{2} \: \text{(sr/píxel)})}
\end{equation}

\begin{equation}
    m_{AB\: \text{JWST}} = - 2,5\log_{10}{(\Phi_{\text{JWST}})} + \text{ZP}
\end{equation}

donde PIXELSCALE es la escala de píxeles de la tabla \ref{tab:jwst} convertida a radianes/píxel y \(\Phi_{\text{JWST}}\) el flujo total en \(\text{MJy}\cdot \text{píxel}/\text{sr}\) (resultado de integrar de manera directa medidas en MJy/sr sobre un área definida por píxeles). Por su parte, para el cálculo de la magnitud en MUSE utilizamos la definición de la magnitud AB \citep{okeSecondaryStandardStars1983}

\begin{equation}
    m_{AB} = - 2,5\log_{10}{(\Phi_{\nu})} + 48,60
\end{equation}

siendo \(\Phi_\nu\) el flujo espectral en el espacio de frecuencias. Dado que los datos de MUSE se ofrecen sobre longitudes de onda (sección \ref{sec:muse}), para obtener la magnitud AB calculamos

\begin{equation}
    m_{AB\: \text{MUSE}} = - 2,5\log_{10}{\bigg(\frac{\Phi_{MUSE} \, \lambda^2}{c}\bigg)} + 48,60
\end{equation}

Los datos del flujo están sujetos a varias fuentes de error. Existen dos principales: la incertidumbre de Poisson asociada al número de cuentas medido por el detector, para la cual debemos tener en cuenta la ganancia de la cámara asociada, y el ruido asociado a cada píxel. Este ruido no es constante, sino que viene determinado por los llamados \enquote{mapas de pesos} (\textit{weighting maps}) que se generan mediante la \textit{pipeline} de cada telescopio. En concreto, nosotros usamos un mapa RMS, que indica este peso en unidades de desviación típica de la imagen. \par

con \(\Phi_{\text{MUSE}}\) el flujo total de un área en \(\text{píxel} \cdot \text{erg}\: \text{cm}^{-2}\: \text{s}^{-1}\: \text{\AA}^{-1}\).
\subsubsection{Selección de la región de estudio} \label{sec:mascara}
Una vez definido el procedimiento a seguir para medir el brillo de un área, queda establecer las regiones sobre las que aplicarlo. En el apartado \ref{sec:contorno} describíamos la morfología de las dos shells que pudimos identificar en la imagen tomada con el filtro F277W de JWST. Debido a su pequeño tamaño y baja luminosidad, la shell occidental puede presentar dificultades para su estudio. Por lo tanto, en lo que resta de este trabajo nos centraremos en la shell oriental. \par

No obstante, como señalamos en el apartado \ref{sec:galaxias} y la figura \ref{fig:galaxias}, en la shell oriental están presentes varias galaxias que no podemos incluir en el análisis. Aunque exploramos otras posibilidades, tal y como exponemos en el anexo \ref{sec:chefs}, el procedimiento más sencillo para eliminar su influencia en las propiedades de la shell es aplicar una máscara sobre la región en la que se encuentran. El inconveniente de este enfoque es que corremos el riesgo de reducir en exceso el área de estudio y, como consecuencia de ello, trabajar con un flujo excesivamente bajo y dificultar la obtención de resultados concluyentes. Por ello la máscara debe ser mínima. \par


Para evaluar si una máscara es adecuada, tomamos como referencia una región en el centro de la shell situada entre \(+00^{\circ} 05^{\prime}22^{\prime\prime}.12\) y \(+00^{\circ} 05^{\prime}23^{\prime\prime}.32\) de declinación y con un área de 3,2 arcsec\(^{2}\) (44,3 kpc\(^{2}\)). Esta región no se ve afectada por la influencia de las galaxias. Posteriormente se compara el flujo de la shell enmascarada en cada filtro de JWST con el de la región de referencia. Si el cociente entre ambos se mantiene constante en todos los filtros, la máscara se considera apta para medir las propiedades de la shell. En este caso probamos dos máscaras que eliminan el área cubierta por las galaxias a partir de cuatro elipses. Una de las máscaras es más conservadora y cubre un área mayor sobre la shell (\enquote{máscara 1}), y otra permite un mayor paso del flujo (\enquote{máscara 2}). Representamos estas máscaras sobre el área que definimos de la shell en la figura \ref{mascaras}, junto con la región de referencia que describimos. En la figura \ref{cocientes} mostramos el cociente entre el flujo de la shell con las dos máscaras y el flujo de la región central de referencia. Comprobamos que con la primera máscara el cociente se mantiene constante para todas las longitudes de onda, señal de que su flujo es representativo. En cambio, en la segunda hay un exceso evidente de emisiones en el canal LW, lo que indica que estamos sufriendo una ligera contaminación de las galaxias presentes sobre la shell. Sabemos que una de esas galaxias se encuentra a un redshift muy superior, por lo que este resultado era esperable \nota{y por la PSF, añadiré una explicación de la PSF si hace falta}. La máscara seleccionada para todas nuestras medidas del flujo es, entonces, la primera. \nota{meter el dato del área} \par

\begin{figure}[h!]
\centering
\includegraphics[trim={0 0 0 0}, clip, width=0.3\textwidth]{mascara1.png}
\includegraphics[trim={0 0 0 0}, clip, width=0.3\textwidth]{mascara2.png}
\includegraphics[trim={0 0 0 0}, clip, width=0.3\textwidth]{mascarasimple.png}
\caption{\nota{escribir pie de figura}  \label{mascaras}}
\end{figure}
\noindent


\begin{figure}[h!]
\centering
\includegraphics[trim={0 0 0 0}, clip, width=0.55\textwidth]{cocientes.png}
\caption{\nota{escribir pie de figura} \nota{corregir unidades a la izquierda}  \label{cocientes}}
\end{figure}
\noindent

Una vez definida el área sobre la que integraremos en JWST, debemos trasladar dicha área a las imágenes de los otros telescopios. Para ello aprovechamos la información presente en el \textit{header} de cada archivo acerca de las coordenadas WCS, y utilizamos las funciones \textit{wcs\_pix2world} y \textit{wcs\_world2pix} del paquete \textit{astropy} de Python para convertir la información sobre la localización en píxeles de un objeto de un telescopio a otro. \par

La región que utilizaremos como referencia para medir el flujo en una zona sin shells es la misma que mostramos en naranja en la figura \ref{color}. Para garantizar que estamos comparando áreas semejantes, le aplicaremos la misma máscara que hemos definido en este apartado.


\subsubsection{Ajuste de la SED. Cálculo de parámetros físicos.}
CIGALE \citep[Code Investigating GALaxy Emission; ][]{boquienCIGALEPythonCode2019,burgarellaCEERSPossiblyForging2025} es un software que toma las medidas espectrofotométricas del brillo de una población estelar y las ajusta a un modelo de SED. Dicho modelo depende de una serie de propiedades de la población, tales como la masa estelar, la metalicidad o la tasa de formación estelar (SFR). Por lo tanto, CIGALE es una herramienta excelente para elaborar una descripción física completa a partir de los datos que obtendremos del flujo de la shell y de la región de referencia.\par

CIGALE requiere una serie de parámetros de configuración para garantizar que produce un modelo que se ajuste a los datos espectrofotométricos que medimos. En nuestro caso, consideramos un historial de formación estelar (SFH) retardado y el modelo de síntesis de población estelar (SSP) de \citet{bruzualStellarPopulationSynthesis2003}. Incluimos también las emisiones nebulares. El polvo se trata, por un lado, con el modelo de atenuación de \citet{calzettiDustContentOpacity2000}, y por otro con el de emisión de \citet{daleTwoparameterModelInfrared2014}. Por tratarse de una shell, no consideramos núcleos activos galácticos (AGN) ni emisiones de rayos X. Las propiedades que obtendremos para las dos regiones a partir del modelo de SED más probable propuesto por CIGALE serán: la masa estelar \(M_{stars}\), la masa del gas \(M_{gas}\), la fracción de luminosidad infrarroja de AGN \(frac(\text{AGN})\), el enrojecimiento provocado por el polvo interestelar E(B-V), la metalicidad logarítmica \(12 + \log{(\text{O}/\text{H})}\), la fracción metálica del gas ionizado \(Z_{\text{gas}}\), el parámetro adimensional de ionización del gas \(\log{U}\) (dependiente del número de fotones ionizantes disponibles por cada átomo de H), la tasa de formación estelar SFR, la edad de la población estelar y la luminosidad infrarroja total \(L_{TIR}\). Con todos ellos podremos caracterizar la shell de manera muy precisa.

\nota{aquí incluiré un diagrama de flujo del trabajo}